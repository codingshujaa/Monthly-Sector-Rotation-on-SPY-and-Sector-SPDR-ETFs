# Scripts

This folder is small on purpose.

The full project logic stays inside the final Quarto file because that is the main submission file.

- `render_report.R` renders the final report again if needed.
